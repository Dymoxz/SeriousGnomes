using UnityEngine;

public class Pond : Entity


{
    [SerializeField] protected new double soilQuality = 0f;
    [SerializeField] protected new double plantDiversity = 0f;
    [SerializeField] protected new double groundwaterLevel = 5f;
    [SerializeField] protected new double biodiversity = 0f
        ;
    [SerializeField] protected new double moneyGenerating = 2f;
    [SerializeField] protected new double health = 5f;
    [SerializeField] protected new double strength = 0f;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
